# Additional dependencies required by the Linux source and portable builds.
# These versions are pinned to avoid pip resolver backtracking or source builds
# on a clean Ubuntu/Python 3.10 installation.
# Keep the shared application dependencies in requirements.txt.
-r requirements.txt
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
noisereduce==3.0.3
